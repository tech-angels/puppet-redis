# Managed by puppet

/var/log/redis-<%= @real_name %>.log {
        daily
        dateext
        missingok
        rotate 5
        compress
        delaycompress
        notifempty
        create 0640 redis adm
}
